define('espo-dental:views/appointment/modals/edit', ['views/modals/edit'], function (Dep) {

    return Dep.extend({

        fullFormDisabled: true,
        sideDisabled: true,
        bottomDisabled: true
    });
});
